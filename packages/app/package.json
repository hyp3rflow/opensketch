{
  "name": "opensketch-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "wasm": "cd ../../crates/engine && wasm-pack build --target web --out-dir ../../packages/app/src/wasm",
    "tauri:dev": "cd ../.. && cargo tauri dev",
    "tauri:build": "cd ../.. && cargo tauri build --bundles app"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.1.0"
  }
}
